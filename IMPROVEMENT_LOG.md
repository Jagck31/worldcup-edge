# Model Improvement Log (Claude, continuous pass)

Each entry: what changed, why, and the measured effect on the time-forward validation slice.
Baseline before this pass (post feature-engineering): **uncalibrated log loss 0.876 / calibrated 0.884**,
Brier ~0.515, accuracy ~0.60, 54 inputs, HistGradientBoosting (xgboost has no Py3.14 wheel).

| # | Change | Rationale | Result |
|---|--------|-----------|--------|
| 0 | Baseline | feature pass complete (elo_expected_home etc.) | logloss raw 0.876 / cal 0.884 |
| 1 | Calibration selection (none/isotonic/temperature, pick best on validation) + added TemperatureCalibrator | Isotonic was *worsening* log loss (0.884 vs raw 0.876); a calibration step should never hurt | Selector chose **none** → calibrated logloss 0.884 → **0.876**. Brier 0.515, acc 0.602. Framework still fits both; temperature will win when the base model is over-confident (e.g. xgboost). |
| 2 | Hyperparameter search (24 configs: lr×leaves×min_leaf×l2) | Live learning curve showed overfitting past ~100 trees | **No gain.** Best config (lr0.03, 7 leaves, msl200, l2=1) → valid 0.877 vs current default 0.876. All good configs converge to shallow 7-leaf trees ~100 iters. Defaults already near-optimal — kept them. |
| 3 | Sample weighting (recency half-life + down-weight friendlies) | World-Cup-relevant matches are recent + competitive | **No gain — worse.** All schemes hurt (recency-3y 0.884, competitive-only 0.879, combined 0.881) vs unweighted 0.878. Equal weights / full data wins; weighting just shrinks effective sample. |

| 4 | **Wired the official 2026 draw + live prediction tracker** | User wants real predictions scored vs actual WC results | Verified all 12 groups (2 sources agree); all 48 teams map to real Elo (added Turkiye→Turkey, Curacao→Curaçao aliases). Sim now runs the OFFICIAL draw. Tracker predicts every group match + scores completed ones. First 4 results: **2/4 correct, log loss 0.915 vs 1.099 uniform** (skill). Out-of-sample USA 4-1 Paraguay missed — model favored Paraguay. |

| 5 | **Host advantage for 2026 fixtures** | Tracker showed host under-rating (predicted Paraguay over USA, who won 4-1 at home) | Mark host (USA/Mexico/Canada) group games non-neutral → home boost. **Live tracker: accuracy 0.50 → 0.75, log loss 0.915 → 0.879.** Out-of-sample USA-Paraguay flips to a correct Home call (OOS log loss 1.078 → 0.824). USA now favored in all its home games. |
| 6 | **Live Polymarket CLOB integration** | User: live executable prices in Trades is "the whole point" | Wired the real Gamma/CLOB client: fetches all 48 "Will <Team> win 2026" champion markets + order books, maps to sim p_champion, prices real executable edges, Kelly-sizes, scans coherence, caches snapshots. Falls back to SAMPLE if offline. Dashboard shows model-vs-market dual bars. **Real edges found: Spain model 24.3% vs market 16.8% (+7.5pp), Argentina 14.3% vs 7.9% (+6.4pp).** Both below $5 min-fill (champion edges small in $). Model more bearish than market on France/Portugal/Brazil. |
| 7 | **Neural network option (sklearn MLP)** | User explicitly wants a NN | Added `model_type: mlp` (impute→scale→64→32 ReLU, early-stopped) selectable in config. Head-to-head vs GBT on identical splits: **MLP log loss 0.8769 / brier 0.5151 vs GBT 0.8780 / 0.5158** — NN marginally better on probabilistic metrics, GBT better on accuracy (0.603 vs 0.597). NN's calibration selector chose **temperature** (raw NN outputs over-confident → calibration framework earns its keep). Both at the same plateau; NN is a legit, competitive option. Dashboard architecture view adapts to show the MLP. |
| 8 | **Paper-trading ledger + gated live execution** | User: capability to actually trade when ready | `PaperLedger` turns positive-edge slate into a paper portfolio with EV/ROI (e.g. Spain edge → EV +$1.04, 54% expected ROI if model right). Live execution is a disabled stub requiring 2 opt-in flags + wallet/USDC/CLOB-key/EIP-712 signing (none wired — manual-only ethos preserved). |
| 9 | **Out-of-sample backtest, sliced by competition** | User: "really backtest this" | Scores the calibrated model on the time-forward holdout (n=4655, 2021-11→2026-06), sliced by type. **Key finding: the model is *better* on major tournaments (log loss 0.839, acc 62.6%, n=2223) than on all internationals (0.876, 60.2%)** — it's most reliable exactly where we bet. Shown in the Model tab. |

| 10 | **Real schedule + tracker UX** | User: chronological order, date+time, separator lines | Loaded the official 72-match schedule (dates + kickoff times, hosts placed at home). Tracker now orders matches chronologically with a Kickoff column and row separator lines. |
| 11 | **Expanded markets: champion + group winners, ALL positive edges** | User: "look for ALL the bets that have edges" | Used Codex's market mapper + fetched the 12 group-winner events. Now prices 95 live markets and surfaces all 42 positive-edge bets (not just ≥5pp). **Honest finding surfaced:** the model gives non-favourites big 'edges' to win their group (Australia 28% vs market 5%) — sim miscalibration (group stage uses only the goal model + no host advantage in the sim), not real edge. Paper ledger restricted to above-threshold/fillable edges only (no more fake 307% ROI). Dashboard carries a prominent caveat. |
| — | **Launcher fix** | `python -m pipeline.orchestrator` failed without PYTHONPATH | Added `run_pipeline.py` (adds src to path); run `python run_pipeline.py`. |

**Next real model fix identified:** the Monte Carlo group stage should use Elo (not just the goal model) for match outcomes and apply host advantage in-sim — that's what's producing the spurious group-winner edges. The champion-market edges (Spain/Argentina vs the market) remain the credible ones.

| 12 | **Recommender + persistent paper-trading bot** | User: run paper trades, whole section, execute edges | Wired Codex's `edge/recommend.py` (rank YES+NO by Kelly impact, risk labels, exposure meters) into Trades. Built `paper_account.py`: a persistent account (data/processed/paper_account.json) that each run settles resolved positions, marks open ones to the live Polymarket mid, and paper-executes new actionable recs. New "$ Paper Account" dashboard tab (equity/cash/PnL/ROI, open positions, settled history). First run: $75 → 6 BUY-NO positions (model's actionable edges are shorting overrated group favourites — same sim-miscalibration caveat applies). |
| 13 | **Last-10-years window + live results into Elo** | User: 1872 is silly; feed live results | `--years 10` (default): 49,409 → 9,544 matches (2016-2026). Live WC results (post-cutoff) now feed the Elo each run so ratings update per matchday; model training still excludes them (tracker stays out-of-sample). Model log loss 0.876 → 0.885 (slightly worse on less data — recency tradeoff; 12-15y would balance). |

| 14 | **FIFA rankings + reliability feature + 12y window** | User asked to add inputs | Wired Dato-Futbol FIFA ranking CSV (derive rank from points, point-in-time join). `rank_points_diff`/`rank_diff` became the #2/#3 features — real orthogonal signal. Added `matches_last_365d` reliability feature. Window 12y. Log loss 0.876 → 0.868. |
| 15 | **FIX: Monte Carlo too flat (Iran 3.7% champ)** | User caught Iran champion absurdly high | Two bugs: (a) 12y Elo window collapsed the rating spread (Spain→Iran gap 164 vs 264) → knockout coin-flips; (b) group stage simulated from goal model only (barely separates teams). Fix: **decouple Elo (full 49,409-match history) from training (12y)**, and make the **scoreline sampler Elo-aware**. Result: Iran 3.7%→**1.4%**, Spain 20.8%→**26.2%**, minnows →~0%, champ probs sum to 100%. Bonus: log loss → **0.858** (best yet, from full-history Elo features). Fewer fake edges (39→25). |

| 16 | **$200 bankroll · settle-date timeline · recommender→paper bot** | User asks | Bankroll $200; paper account opens ~16 positions, mix of BUY YES favourites + BUY NO shorts; each position shows its **settle date** (group winners 2026-06-27, champion 2026-07-20) and the table is a timeline. |
| 17 | **30k sims + live sim visuals + live bracket** | User: more visuals, watch it shape over time, sample bracket | Default 30k sims. Live Run now traces **champion + reach-final + advance** probability trajectories per team (sparklines), a **convergence/movement meter** (→0), throughput/ETA, and a **live sample-tournament bracket** (funnel Last-16→Champion + the champion's path) that refreshes each batch. Sample bracket also shown statically on the Simulation tab. |
| 18 | **Elo MoV autocorrection (538-style)** | User: can Elo be tuned to be more in tune with modern ratings? Measured it. | Built `eval_elo.py` — rebuilds point-in-time Elo under many configs, scores each on a 2023+/2025+ time-forward holdout with proper scoring rules (binary log loss + Brier on home result∈{1,0.5,0}). **Finding: home_adv=75 and K are already near-optimal** (sweeps confirmed — making Elo more "responsive" via higher K helped 2025+ but hurt 2023+; no robust win). **The one real win: autocorrect the margin-of-victory bonus** so favourites don't inflate by thrashing minnows: `mult *= 2.2/(0.0018·rating_diff_winner + 2.2)`. Holdout log loss **0.5469→0.5451** (2023+), accuracy **0.786→0.792** (2025+) — small but consistent across both windows and both metrics. Modern table stays sane (Spain 2124, Argentina 2057, France 2036, England 2025, Brazil 1994). 6/6 Elo tests pass. |
| 19 | **Goal model: opponent-adjusted xG (fixes the "flat xG")** | User: fix the flat xG driving the suspect group-winner edges | The goal model computed attack/defense as raw goals ÷ global average — no opponent adjustment — so unbalanced international schedules compressed it ("flat xG"). Replaced with **iterative Poisson MLE** (`opponent_adjusted=True`): credits goals by the defence faced. Measured on a 12y→holdout (`eval_goal_model.py`): **1X2 log loss 0.906→0.847** (2025+, ~6.5% better, now ~matching the full classifier from scorelines alone); exact-scoreline ll 3.03→2.91; attack-strength stdev 0.357→0.437 (genuinely de-flattened). Old raw method kept behind `opponent_adjusted=False`. 5/5 goal-model tests pass; model log loss held at 0.858 (no regression). |
| 20 | **Correlation-aware portfolio (growth-optimal, not Sharpe)** | User: run the book like a hedge fund; NO-X and YES-Y in a group are ~the same trade | New `edge/portfolio.py`: evaluates every candidate bet's payoff across the 30k simulated tournaments → empirical joint distribution → **growth-optimal (fractional-Kelly) allocation under per-bet (15%) + per-group (25%) caps**. Captures all correlations with zero hand-coding (confirmed NO-Switzerland ≈ YES-Canada at **0.60 corr**). Flags near-duplicate exposures + exact dupes (corr 1.00) and NO/YES opposites (−1.00). **Dropped max-Sharpe** per user — it rewarded low-variance sim noise (reported a bogus 3.94) and ignores ruin; Sharpe kept only as one descriptive number. Wired into the orchestrator + a new "Book" tab. Honest caveat surfaced: the optimizer loads the *suspect* non-favourite group edges — it's only as good as the edges fed in. |
| 21 | **Adjustable sims + live price-watcher** | User: set sims before a Live Run; is it watching prices? | Web app: sims selector (5k–100k) next to Run, passed through `/run?sims=N`. **`price_watcher.py`**: re-fetches the live Polymarket CLOB every N min, re-detects edges + re-optimizes the book from cached sim outcomes (`sim_outcomes.json`) **without retraining** (model probs don't move intraday), writes refreshed markets/portfolio to state, and prints an alert on each new actionable edge. Validated single-pass against the live feed. |
| 22 | **xG-AWARE live Elo updates** | User: use in-depth stats from each WC 2026 match to adjust probabilities | Elo update now blends the scoreline with an xG-"deserved" result: `effective_result = 0.6·result + 0.4·logistic((home_xg−away_xg)/0.9)`, and the MoV margin blends goal-diff + xG-diff (`margin_multiplier` made float-aware). Added optional `home_xg`/`away_xg` to `wc2026_results.csv` (fetched real xG for the 4 played matches via web) + carried through `_live_wc_rows`. **Effect (real): USA's 4-1 on only 1.35 xG vs Paraguay gains 11 fewer Elo than the scoreline alone; Paraguay loses 11 fewer.** Propagates to the sim (USA champion/win-group nudged down). Only post-cutoff live matches get it (the other 3 are in the martj42 dataset). Codex's FBref ingestion can auto-populate xG going forward. 11/11 Elo+goal tests pass. |

**Conclusion after #1–#3: the model is at a data-limited plateau (~0.876 log loss, vs 1.099 uniform).** [Superseded: full-history Elo + 12y training + FIFA rankings reached 0.858.] International 1X2 is heavily Elo-determined; further log-loss gains won't come from model config. Headroom is in (a) real-data realism — wiring the actual 2026 group draw/fixtures to replace the demo draw, and (b) the goal model / scoreline sub-markets (Codex's lane). Pivoting improvement effort accordingly.

| 23 | **Live match tracker (auto re-score + forward refresh)** | User: track games live to see if predictions hit, update after each hit | New `live_tracker.py` polls the live results feed (**TheSportsDB league 4429**, free key, no token — its season-2026 feed returns exactly our 6 hand-entered results, so it IS the source). Each cycle: pull events → append newly-finished to `wc2026_results.csv` (xG preserved) → re-score and refresh. **Key design — freeze at kickoff:** a finished game is scored against its *pre-match* prediction, never re-predicted with Elo that already absorbed the result (look-ahead leakage that would inflate accuracy). Only still-scheduled fixtures move forward as completed results feed Elo. Engine (`src/pipeline/live_tracker.py`) trains the model once, then recompute is cheap per poll. Verified by injecting Haiti 3-0 Scotland: scored as MISS on the frozen 46% Scotland pick (frozen prob unchanged to 1e-9), while downstream Scotland-Morocco shifted 0.492→0.587 to Morocco from the Elo update; `--no-forward` freezes downstream too. Writes `tracker` + a `tracker.live` block into `dashboard_state.json`; the web dashboard (auto-refresh 30s) shows ✓/✗ + a live-feed banner. New `src/ingest/livescores.py` client (config/env-driven, graceful degradation). 67/67 tests pass. |
